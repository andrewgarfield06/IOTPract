// Blink on Arduino
const int ledPin = 13; // built-in LED on many boards

void setup() {
  pinMode(ledPin, OUTPUT);
}

void loop() {
  digitalWrite(ledPin, HIGH); // LED on
  delay(500);                 // 500 ms
  digitalWrite(ledPin, LOW);  // LED off
  delay(500);                 // 500 ms
}
